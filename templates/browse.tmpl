<!DOCTYPE html>
<html>
  <head>
    <title>Chrss - browse open matches</title>
    <link rel="stylesheet" type="text/css" href="/static/main.css">
    <link rel="icon" href="/static/favicon.ico" type="image/x-icon">
  </head>
<body>
  
<div id="browse">
  <h1 id="banner">Let's Play Chrss!</h1>
  <h2><a href="/guide">How to play</a><h2>

    <a href="/createMatch">Create match</a>
    <div id="match_list">
      {{if .Open}}
      <h3>Open matches:</h3>
      <p id="join_instruction">When connecting to a match, you'll get an error if someone has already connected as that color. Refresh this page to see which matches are open. If you leave a match, you can rejoin by 
       simply going to the same URL. (The dev link is for playing both sides on one page for development purposes.)</p>
      <ul>
          {{range .Open}}
              <li>{{.Name}} ({{.Elapsed}} ago)
                &nbsp;{{if .WhiteOpen}}<a href="/match/{{.Name}}/white">join white</a> {{else if .BlackOpen}} <a href="/match/{{.Name}}/black">join black</a> {{end}}
                &nbsp;{{if and .WhiteOpen .BlackOpen}}<a href="/dev/{{.Name}}">dev</a>{{end}}
              </li>
          {{end}}
      </ul>
      {{end}}
      {{if .Full}}
      <h3>Full matches:</h3>
      <ul>
          {{range .Full}}
              <li>{{.Name}} ({{.Elapsed}} ago)</li>
          {{end}}
      </ul>
      {{end}}
    </div>
</div>


</body>
</html>
